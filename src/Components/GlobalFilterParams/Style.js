import { makeStyles } from '@mui/styles'

const useStyles = makeStyles((theme) => ({
    button: {},
    TextField: {
        width: '15rem',
        margin: '0.5rem',
    },
    DateBox: {
        width: '15rem',
        margin: '0.5rem',
    },
    TextFieldDate: {
        width: '7rem',
        margin: '0.2rem',
    },
}))

export default useStyles
