import { makeStyles } from '@mui/styles'

const useStyles = makeStyles((theme) => ({
    button: {
        width: '100%',
    },
    TextField: {
        width: '100%',
    },
    TextFieldDate: {
        width: '100%',
    },
}))

export default useStyles
